# a template app for standalone projects
- room for persistence
- androidx subclasses (AppCompatActivity, Fragment) base classes
- kotlin coroutines testing support
- retrofit
- instrumentation test support with a managed device
- unit tests with junit
- hilt for DI + plain dagger for submodules
